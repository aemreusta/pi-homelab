---
- name: Create storage directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ storage_owner }}"
    group: "{{ storage_group }}"
    mode: "0755"
  loop: "{{ storage_directories }}"
